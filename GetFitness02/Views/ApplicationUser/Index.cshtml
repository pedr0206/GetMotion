@model LoginViewModel
@using Microsoft.AspNetCore.Identity
@using GetFitness02.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@{
    ViewData["Title"] = "Index";
}

<h1 class="text">User Profile</h1>
<div class="img-profile">
    <img src="~/images/user.png" width="150"/>
</div>
<div>
    @if (SignInManager.IsSignedIn(User))
    {
        <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm">
            <div style="text-align:center;">
                <div style="padding-left:20px;">
                    <a asp-area="" asp-controller="Home" asp-action="Index">Hello! </a> @UserManager.GetUserName(User)
                </div>
                @*<div>
                    @Html.DisplayNameFor(model => model.FullName)
                </div>*@
                <div class="btn-submit">
                    <button type="submit" class="btn-submit">Log out</button>
                </div>
            </div>
        </form>
    }
    else
    {
        <div>
            <div class="btn-submit">Already have an account?<strong><a asp-area="" asp-controller="Account" asp-action="Login"> Log in</a></strong></div>
        </div>
    }
</div>

